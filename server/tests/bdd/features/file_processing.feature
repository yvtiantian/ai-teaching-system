# language: zh-CN
功能: 参考资料文件处理
  作为一名教师
  我希望系统能正确处理各种格式的参考资料
  以便 AI 能基于资料内容出题

  场景: 成功提取 TXT 文件内容
    假设 存在一个 TXT 文件 内容为 "Python是一门解释型编程语言"
    当 系统提取文件文本
    那么 应返回包含 "Python" 的文本

  场景: 成功提取 PDF 文件内容
    假设 存在一个 PDF 文件 包含文字内容
    当 系统提取文件文本
    那么 应返回非空文本

  场景: 不支持的文件类型应报错
    假设 存在一个 MIME 类型为 "application/zip" 的文件
    当 系统提取文件文本
    那么 应抛出 ValueError 提示不支持的文件类型

  场景: 文件内容超过长度限制应被截断
    假设 存在一个 TXT 文件 内容为 60000 个字符
    当 以最大长度 100 提取文件文本
    那么 文本长度不应超过 110
    并且 文本应以截断标记结尾

  场景: 存储桶白名单校验
    假设 存储路径为 "evil-bucket/some/path.txt"
    当 系统尝试下载文件
    那么 应抛出 ValueError 提示不允许访问的存储桶

  场景: 无效存储路径应报错
    假设 存储路径为 "no-slash-path"
    当 系统尝试下载文件
    那么 应抛出 ValueError 提示无效的存储路径
